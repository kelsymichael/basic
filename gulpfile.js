const gulp = require('gulp');

// second is where folder is located
// each filename = a named task
// as if we used gulp.task etc.
require('gulp-load-tasks')('gulp_tasks');

// gulp.task('default', () => console.log('gulp is running!'));
